//chrome.runtime.sendMessage(document.getElementsByTagName('title')[0].innerText);
/*chrome.browserAction.onClicked.addListener(function(0) {
  chrome.tabs.executeScript({
    code: 'document.body.style.backgroundColor="red"'
  });
});*/